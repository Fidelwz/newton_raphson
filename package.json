{
  "name": "newton_raphson_root",
  "private": true,
  "scripts": {
    "heroku-prebuild": "cd client-react && npm install --include=dev",
    "heroku-postbuild": "cd client-react && npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}
